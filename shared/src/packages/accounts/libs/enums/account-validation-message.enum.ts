const AccountValidationMessage = {
  NAME_REQUIRE: 'Name is required',
  AMOUNT_REQUIRE: 'Amount is required',
  CURRENCY_REQUIRE: 'Currency is required',
} as const;

export { AccountValidationMessage };
